# タスクリスト

## 進行中

（なし）

## バックログ

### Step 2: バグ修正 + シンプルなUI改善

- [x] 一時停止 → 再生再開時に画面が暗いままになる問題を修正する（暗転解除を早める）
- [x] バッファリング中（読み込み待ち）はスピナーを表示する（フリーズと区別できるように）
- [x] 各画面のデフォルトフォーカスを左上のファイル・フォルダ項目にする（設定アイコンが選ばれないように）

### Step 3: フォーカス制御

- [x] NAS情報登録画面: テキストボックスは決定ボタン押下時のみ入力モードに入るようにする（フォーカス時の自動入力を無効化）
- [x] 戻るボタンで画面遷移したときは直前に選択していた項目にフォーカスを復元する

### Step 4: アプリ内アップデート

- [ ] 設定画面にアップデート確認ボタンを追加する
  - GitHub API (`naito60192033/HanaBi`) で最新リリースを取得
  - 現在の `BuildConfig.VERSION_NAME` と比較して新バージョンがあれば通知
  - APK をダウンロードし `FileProvider` + `Intent(ACTION_VIEW)` でシステムインストーラーを起動
  - 必要権限: `REQUEST_INSTALL_PACKAGES`、`FileProvider` 設定

### Step 5: 高度な動画再生機能

- [ ] 再生中: 上ボタンで次チャプター・下ボタンで前チャプターへジャンプ（チャプターなしの場合は10分単位スキップ）
- [ ] 一時停止中: 字幕・音声トラック選択UIを表示する（一時停止中は十字キースキップを無効にして競合を防ぐ）
- [ ] 再生中のUI表示をYouTubeアプリを参考にリデザインする

## 完了

- [x] プロジェクト初期セットアップ
- [x] NAS接続・UI・動画再生の動作修正
- [x] README追加
- [x] ファイルソートを自然順ソートに変更する（10, 100 が正しい順序になるよう）
- [x] サムネイル生成位置を各動画ファイルの先頭から5%進んだフレームにする
- [x] サムネイル生成を非同期処理にする（UI スレッドをブロックしない）
- [x] ファイル名に `#` 等の URI 特殊文字が含まれる場合にサムネイルが表示されないバグを修正
